const images = {
    logoIcon: require('../img/logo-tiktok.svg').default,
    logoFull: require('../img/logo-full.svg').default,
    search: require('../img/search.svg').default,
    noImage: require('../img/image-not-found.svg').default,
};

export default images;
